# React Native | Express Server | MongoDB Connection 

> An express server connected to a react-native client

## Usage

```
# Install dependencies
npm install

# Install client dependencies
npm run client-install

# Run client-admin and server (Visit http://localhost:3000)
npm run dev

# Run server only (:5000)
npm run server

# Run client-admin only (:)
npm run client
```